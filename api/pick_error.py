def can_report_error(get_pt, pickticket_id, errors):
    # get pick ticket, existing_errors

    # if status = Picked OR ERROR
    # if picks - existing_errors + new_errors >= 0
        # produce ERROR action to worker
    # else false

    return True